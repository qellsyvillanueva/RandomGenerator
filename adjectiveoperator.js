var adjectives =
      [ "conceited",
        "ignorant",
        "untalented"];
        
var person = prompt("What is your name?");

var choose = (Math.floor(Math.random() * adjectives.length));

var

window.alert(total);